//! The pipeline manager manages pipeline definitions and their validity. 
//! Furthermore modules for implementing individiual pipeline steps are managed to create flexible pipelines.
//! The execution of pipelines is managed by the jobs module. 

pub mod manager;
pub mod interface;
